import { GlobalEventConfiguration } from "../types";

export const appStart: GlobalEventConfiguration = {
    name: 'app-start',
    label: 'App Start',
    group: '',
    inputs: [],
    events: [],
};
